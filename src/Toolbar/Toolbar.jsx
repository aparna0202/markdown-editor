import React from "react";
import ReactMarkdown from "react-markdown";

const Toolbar = () => {
  return <ReactMarkdown> [Duck Duck Go](https://duckduckgo.com)</ReactMarkdown>;
};

export default Toolbar;
